name: Daily Competitor Scan

on:
  schedule:
    - cron: '0 1 * * *'  # UTC 1:00 = JST 10:00
  workflow_dispatch:  # Manual trigger

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --no-cache-dir -r packages/scraper/requirements.txt
          pip install --no-cache-dir -r packages/analyzer/requirements.txt

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ hashFiles('packages/scraper/requirements.txt') }}

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: playwright install chromium

      - name: Install Playwright deps
        run: playwright install-deps chromium

      - name: Run scan
        id: run_scan
        continue-on-error: true
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
        run: |
          python packages/scraper/src/main.py

      - name: Notify on failure
        if: steps.run_scan.outcome == 'failure'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{"text":":warning: Competitor Monitor daily scan failed. Check GitHub Actions logs."}' \
            $SLACK_WEBHOOK_URL

      - name: Run press release scan
        id: run_press
        continue-on-error: true
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          PRESS_NOTIFICATIONS_DISABLED: "true"
        run: |
          python packages/scraper/src/press_main.py

      - name: Notify press scan failure
        if: steps.run_press.outcome == 'failure'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{"text":":warning: Press Release Monitor scan failed. Check GitHub Actions logs."}' \
            $SLACK_WEBHOOK_URL

      - name: Run HOME'S structure scan
        id: run_homes
        continue-on-error: true
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python packages/scraper/src/own_site_scanner.py

      - name: Notify HOME'S scan failure
        if: steps.run_homes.outcome == 'failure'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{"text":":warning: HOME'\''S structure scan failed. Check GitHub Actions logs."}' \
            $SLACK_WEBHOOK_URL

      - name: Fail job if any scan failed
        if: steps.run_scan.outcome == 'failure' || steps.run_press.outcome == 'failure' || steps.run_homes.outcome == 'failure'
        run: exit 1
